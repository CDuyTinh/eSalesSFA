<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/Sfa.Card"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/space_md">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/productName"
                style="@style/Sfa.Text.Body"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:maxLines="2"
                android:ellipsize="end"
                android:textStyle="bold"
                tools:text="Nước giải khát loại 12" />

            <TextView
                android:id="@+id/removeButton"
                style="@style/Sfa.Text.Tiny"
                android:layout_marginStart="@dimen/space_sm"
                android:padding="@dimen/space_xs"
                android:textColor="@color/brandRed"
                android:text="@string/order_remove" />
        </LinearLayout>

        <TextView
            android:id="@+id/qtyPrice"
            style="@style/Sfa.Text.Tiny"
            android:layout_marginTop="2dp"
            tools:text="5 THUNG × 240.000" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_sm"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <!-- Nhãn khuyến mãi nền nhạt để nhân viên thấy ngay dòng nào được
                 giảm mà không phải so từng con số. -->
            <TextView
                android:id="@+id/discount"
                style="@style/Sfa.Text.Tiny"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:paddingHorizontal="@dimen/space_sm"
                android:paddingVertical="2dp"
                android:background="@drawable/bg_chip_green"
                android:textColor="@color/stateGreen"
                tools:text="KM −60.000" />

            <TextView
                android:id="@+id/lineTotal"
                style="@style/Sfa.Text.Body"
                android:layout_marginStart="@dimen/space_sm"
                android:textStyle="bold"
                tools:text="1.140.000" />
        </LinearLayout>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
