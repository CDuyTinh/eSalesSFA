<?xml version="1.0" encoding="utf-8"?>
<!--
  Thẻ khách hàng theo bố cục bản eSales gốc: ảnh cửa hàng bên trái, tên kèm nhãn
  mã ở hàng đầu, các dòng thông tin phụ có biểu tượng dẫn, và nút Check-in nằm
  góc dưới bên phải.
-->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/Sfa.Card"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/space_md">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <FrameLayout
                android:layout_width="@dimen/thumb_size"
                android:layout_height="@dimen/thumb_size"
                android:background="@drawable/bg_thumb">

                <!-- Chữ cái đầu tên nằm dưới ảnh: master data nhiều khách chưa có
                     ảnh, mà ô trống trơn nhìn như đang tải lỗi. -->
                <TextView
                    android:id="@+id/initial"
                    style="@style/Sfa.Text.Title"
                    android:layout_gravity="center"
                    android:textColor="@color/textGray"
                    tools:text="M" />

                <com.google.android.material.imageview.ShapeableImageView
                    android:id="@+id/photo"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:scaleType="centerCrop"
                    android:visibility="gone"
                    app:shapeAppearanceOverlay="@style/Sfa.Thumb"
                    tools:visibility="visible" />
            </FrameLayout>

            <LinearLayout
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/space_md"
                android:orientation="vertical">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center_vertical">

                    <TextView
                        android:id="@+id/name"
                        style="@style/Sfa.Text.Body"
                        android:layout_width="0dp"
                        android:layout_weight="1"
                        android:maxLines="2"
                        android:ellipsize="end"
                        android:textStyle="bold"
                        tools:text="Cửa hàng Minh Anh 12" />

                    <TextView
                        android:id="@+id/code"
                        style="@style/Sfa.Badge"
                        android:layout_marginStart="@dimen/space_sm"
                        android:background="@drawable/bg_badge_gray"
                        tools:text="44100208" />
                </LinearLayout>

                <!-- Thứ tự ghé không có biểu tượng dẫn, giống bản gốc. -->
                <TextView
                    android:id="@+id/visitOrder"
                    style="@style/Sfa.Text.Tiny"
                    android:layout_marginTop="@dimen/space_xs"
                    tools:text="Thứ tự viếng thăm: 1" />

                <LinearLayout
                    android:id="@+id/channelRow"
                    style="@style/Sfa.CustomerRow">

                    <TextView
                        style="@style/Sfa.Text.Tiny"
                        android:text="@string/glyph_store" />

                    <TextView
                        android:id="@+id/channel"
                        style="@style/Sfa.CustomerRow.Text"
                        tools:text="Mỹ Phẩm" />
                </LinearLayout>

                <LinearLayout
                    android:id="@+id/phoneRow"
                    style="@style/Sfa.CustomerRow">

                    <TextView
                        style="@style/Sfa.Text.Tiny"
                        android:text="@string/glyph_phone" />

                    <TextView
                        android:id="@+id/phone"
                        style="@style/Sfa.CustomerRow.Text"
                        tools:text="0901234567" />
                </LinearLayout>

                <LinearLayout
                    android:id="@+id/addressRow"
                    style="@style/Sfa.CustomerRow">

                    <TextView
                        style="@style/Sfa.Text.Tiny"
                        android:text="@string/glyph_pin" />

                    <TextView
                        android:id="@+id/address"
                        style="@style/Sfa.CustomerRow.Text"
                        android:maxLines="2"
                        android:ellipsize="end"
                        tools:text="12 Đường số 3, Quận 1, TP.HCM" />
                </LinearLayout>

                <LinearLayout
                    android:id="@+id/assigneeRow"
                    style="@style/Sfa.CustomerRow">

                    <TextView
                        style="@style/Sfa.Text.Tiny"
                        android:text="@string/glyph_clock" />

                    <TextView
                        android:id="@+id/assignee"
                        style="@style/Sfa.CustomerRow.Text"
                        tools:text="Viếng thăm bởi NV001 - Trần Văn A" />
                </LinearLayout>
            </LinearLayout>
        </LinearLayout>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_sm"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <!-- Nhãn trạng thái ôm sát nội dung, khoảng trống đẩy nút sang phải —
                 nhãn kéo dài nửa thẻ nhìn nặng hơn thông tin nó mang. -->
            <TextView
                android:id="@+id/status"
                style="@style/Sfa.Text.Tiny"
                android:paddingHorizontal="@dimen/space_md"
                android:paddingVertical="3dp"
                android:gravity="center"
                android:background="@drawable/bg_chip_orange"
                tools:text="Chưa ghé" />

            <View
                android:layout_width="0dp"
                android:layout_height="1dp"
                android:layout_weight="1" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/checkInButton"
                style="@style/Sfa.Button.Compact"
                android:layout_marginStart="@dimen/space_sm"
                tools:text="@string/action_check_in" />
        </LinearLayout>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
