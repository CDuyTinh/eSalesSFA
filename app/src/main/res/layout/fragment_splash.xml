<?xml version="1.0" encoding="utf-8"?>
<!--
  Bố cục theo splash_screen.xml của bản eSales gốc: logo giữa màn hình, một dòng
  chân trang, tất cả đặt trên nền đỏ có hoạ tiết dải sóng.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_brand_gradient">

    <com.tinhcd.esalessfa.core.ui.RibbonPatternView
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <LinearLayout
        android:id="@+id/logoGroup"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical"
        android:gravity="center_horizontal">

        <ImageView
            android:layout_width="@dimen/logo_size"
            android:layout_height="@dimen/logo_size"
            android:src="@drawable/ic_logo_mark_light"
            android:importantForAccessibility="no" />

        <TextView
            style="@style/Sfa.Text.Brand"
            android:layout_marginTop="@dimen/space_md"
            android:text="@string/brand_name"
            android:textSize="@dimen/text_brand_splash"
            android:textColor="@color/white" />
    </LinearLayout>

    <TextView
        android:id="@+id/footerText"
        style="@style/Sfa.Text.Small"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="@dimen/splash_footer_margin_bottom"
        android:text="@string/splash_footer"
        android:textColor="@color/white" />
</FrameLayout>
